# Copia di riserva dell'index.html originale (prima della divisione in
# HTML + CSS + JS). Serve solo in locale come rete di sicurezza: online
# non deve finire, appesantirebbe il repository senza servire al sito.
index.html.backup

# Note di lavoro del progetto, tenute solo in locale.
CLAUDE.md
RIASSUNTO_PROGETTO.md

# Inserimento dei talenti: contiene i testi dei manuali (copyright). Il repo è
# pubblico, quindi questi file restano SOLO in locale / da lanciare su Supabase.
sql/04_talenti_seed.sql
sql/06_talenti_full.sql

# Inserimento dei privilegi di classe/sottoclasse: idem, testi dei manuali.
sql/09_privilegi_seed.sql
sql/10_privilegi_base_seed.sql
